# Backend Server Fix - Progress Tracker

## Plan Steps:
1. [x] Create `api/ml_routes_new_fixed.py` with fixed model loading (.keras)
2. [x] Update `requirements.txt` to add joblib and dill
3. [x] Install dependencies
4. [ ] Test server: `cd cnc_automation_project/backend & uvicorn app:app --host 0.0.0.0 --port 8000 --reload`
5. [ ] Verify endpoints (e.g. http://localhost:8000/api/ml/model-info)

**Next: Run the server command in backend dir to test.**

